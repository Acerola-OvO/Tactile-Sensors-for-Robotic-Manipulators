# Legacy prototype

`legacy_prototype.py` is the compact script used during the original experiments. It is preserved for historical reproducibility. New users should run `tactile_visualizer.py`, which adds validated frame parsing, configurable serial ports, clean shutdown, simulation mode, and clearer normalization options.
